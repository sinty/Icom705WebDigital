# Единый перевод строки в репозитории независимо от системы разработчика:
# иначе с Windows приезжает CRLF и diff-ы становятся нечитаемыми.
* text=auto eol=lf

# Эталонные записи и сборки трогать нельзя ни при каких обстоятельствах
*.wav  binary
*.wasm binary
*.png  binary
*.jpg  binary
